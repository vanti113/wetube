extends layouts/main.pug
include mixins/videoBlock.pug

block content
  .videos 
    each item in videos
      +videoBlock({
        title : item.title,
        views : item.views,
        videoFile : item.videoFile
      }) 